# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19

# --- Config & validation ---
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Database ---
# Default: SQLite (built-in, tanpa driver tambahan).
# Kalau mau PostgreSQL lagi, install psycopg2-binary dan set DATABASE_URL_OVERRIDE.
sqlalchemy==2.0.36
alembic==1.14.0

# --- Vector database ---
# extra [local] = mode embedded (file-based, tanpa server Qdrant).
qdrant-client[local]==1.12.1

# --- Embedding lokal (RAG) ---
fastembed==0.4.1

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# --- File processing (ingestion) ---
pandas==2.2.3
openpyxl==3.1.5
pypdf==5.1.0
tabulate==0.9.0

# --- LLM (Ollama Cloud / lokal) ---
ollama==0.4.4
httpx==0.27.2

# --- Security & Rate Limiting ---
slowapi==0.1.9
itsdangerous==2.2.0

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0
